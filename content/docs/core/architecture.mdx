---
id: architecture
slug: core/architecture
label: Architecture
title: Architecture
description: Statics is two coordinated EIP-2535 diamonds behind one ordinary user-action address, with custody and solvency split across clear boundaries.
updated: 2026-09-05
order: 20
---

**In plain language:** users talk to one main contract for baskets, positions,
lending, and pool management; Dollar collateral and solvency live in a second contract so
those books stay isolated.

Statics is built as two EIP-2535 diamonds that share one timelock but keep
distinct custody and solvency boundaries. Most user actions route through a
single address — `StaticsDiamond` — while the Statics Dollar's collateral and
health machinery stay isolated in a separate `StaticsDollarCoreDiamond`.

## Two coordinated diamonds

| Diamond | Role | Holds |
| --- | --- | --- |
| `StaticsDiamond` | User gateway, PositionNFT, baskets, rewards, lending, flash loans, protocol pools, and the Dollar gateway | Basket vaults, fee account, staking custody, Genesis reward account, voluntary LP custody |
| `StaticsDollarCoreDiamond` | Dollar collateral, issuance, health, transitions, insurance, and recovery | Core collateral and insurance |

`StaticsDiamond` is the ordinary integration address. It is simultaneously the
basket action surface, the PositionNFT ERC-721 contract, the Dollar gateway, the
Core periphery, the Core fee receiver, and the managed recovery holder. There is
no separate user router or PositionNFT proxy.

## Why two diamonds

Dollar solvency is a separate custody and accounting boundary even though the
user-facing gateway lives on `StaticsDiamond`. Keeping the Dollar's collateral,
insurance, and recovery state inside `StaticsDollarCoreDiamond` means a Diamond
cut or failure on the user surface cannot directly restate Dollar books, and
vice versa. Both diamonds are owned by the same `StaticsTimelock`, so governance
stays unified while custody stays separated.

:::note
This separation does not merge Dollar collateral held by Core with Diamond
custody. Only fees explicitly transferred to the periphery enter shared
reservations.
:::

## Topology

```text Statics two-diamond layout
Users and integrators
        |
        v
StaticsDiamond
├── EIP-2535 dispatch and governance
├── PositionNFT ERC-721
├── basket creation, mint, redeem, collateral, lending, and flash loans
├── Morpho collateral, USDstx borrowing, and loss synchronization
├── global staking, reward claims, and treasury fee distribution
├── Statics Dollar typed gateway, Risk liquidity, and funded incentives
├── shared custody reservations
└── basket canonical pools, general pools, and borrow-to-liquidity
        |
        +-------------------------> StaticsDollarCoreDiamond
        |                           ├── collateral and issuance
        |                           ├── health and transitions
        |                           ├── insurance and recovery
        |                           └── risk-series state
        |
        +-------------------------> StaticsSwapFeeHook
        |                           ├── bilateral swap fees
        |                           └── hook-owned permanent liquidity
        |
        +-------------------------> StaticsLiquidityManager
        |                           ├── normalized protocol-pool validation
        |                           └── typed user PositionManager NFT creation
        |
        +-------------------------> position-specific StaticsMorphoAccount
                                    └── Morpho Blue collateral and debt
```

## What lives where

Physical token locations are intentionally split across contracts so each
accounting boundary can be verified independently:

| Location | Holds |
| --- | --- |
| `StaticsDiamond` | Basket vault backing, the fee account, staking custody, Genesis reward account, and voluntarily custodied LP NFTs |
| `StaticsDollarCoreDiamond` | Dollar collateral and insurance |
| `StaticsSwapFeeHook` | Pending and locked permanent-liquidity assets |
| `StaticsLiquidityManager` | Transaction-scoped user LP inputs only |
| Uniswap v4 `PoolManager` | Basket-canonical and general-pool liquidity under v4 accounting |
| Morpho Blue | Deployed collateral attributed to each position's `StaticsMorphoAccount` |

Direct donations to the Diamond are unreserved and do not inflate backing, fee
claims, or stake. See [Custody and accounting](/docs/core/custody) for the
reservation equation and deficit handling.

## Canonical deployment shape

Current `master` source and fresh-deployment tests expect **34 facets / 283
selectors** on `StaticsDiamond` and **11 facets / 95 selectors** on
`StaticsDollarCoreDiamond`. These counts include the Operators integration,
general-pool lifecycle, Morpho, portfolio, and batched-claim selectors. They
are structural facts for the stated source/deployment revision, not a promise
about every address.

The [earlier public Robinhood release and newer September rehearsal](/docs/reference/robinhood-testnet-deployment)
are distinct snapshots. The downloadable `addresses.json` and ABIs remain pinned
to the earlier release, which has a different shape. These source counts come
from `test/deployment/DeployStatics.t.sol`, not a claim that every published
address has been upgraded.

## Upgradeability

Both diamonds are pre-release EIP-2535 diamonds owned by one timelock. Diamond
cuts are the sole implementation upgrade mechanism. The intended release
lifecycle is upgradeable development followed by an independent audit, reduction
of retained powers, and a final reviewed cut that removes Diamond-cut authority
from both diamonds. See [Governance and lifecycle](/docs/governance/timelock-and-roles).

The hook, PoolManager, PositionManager, and Permit2 bindings remain
version-level dependencies. Current source allows timelocked governance to
replace `StaticsLiquidityManager` with `replaceLiquidityManager`; replacement
revokes approval from the old manager, approves the new manager, and emits the
handoff. It does not replace the hook or Uniswap contracts.
